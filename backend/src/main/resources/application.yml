server:
  port: 8085

spring:
  datasource:
    url: jdbc:h2:file:./data/testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: navirandom222@gmail.com
    password: vlngctrkectpwuly
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: ${JWT_SECRET:EvidenceVaultFixedJwtSecretKeyForDev12345}
  upload:
    dir: ${UPLOAD_DIR:uploads}
    max-size-bytes: ${UPLOAD_MAX_SIZE_BYTES:5242880}
  mail:
    from: ${MAIL_FROM:no-reply@evidence-vault.local}
